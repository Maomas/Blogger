<h1>All Tags</h1>

<ul id="tags">
        <% @tags.each do |tag| %>
                <li>
                        <%= link_to tag.to_s, tag_path(tag) %>

                </li>
        <% end %>
        <%= link_to "Create a new tag", new_tag_path, class:"new_tag" %>

</ul>
 
